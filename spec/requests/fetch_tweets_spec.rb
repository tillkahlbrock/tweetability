require 'spec_helper'

describe "FetchTweets" do
  it "shows the tweets for the pattern after fetch them" do
    tweet_client = double()
    #pattern = create(:pattern)
    #visit pattern_path pattern.id
    #click_link "Fetch Tweets"
    #expect(page).to have_content "Some tweet text"
  end
end
